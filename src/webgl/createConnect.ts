import { Event, Object3D } from "three"
import { NeuronNode } from "../types"

export function createConnect(
  parent: NeuronNode,
  child: NeuronNode
): Object3D<Event> {
  // TODO
  throw new Error()
}
